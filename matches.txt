
LineNumber Line                                                                                                        
---------- ----                                                                                                        
       209     const [isProjectTasksPaneOpen, setIsProjectTasksPaneOpen] = useState<boolean>(false);                   
       503     const isProjectLocked = folderContexts.length > 0 && projects.some(p => p.id === folderContexts[0].id...
      1063                 setIsProjectTasksPaneOpen(true);                                                            
      1075                 setIsProjectTasksPaneOpen(true);                                                            
      1398         const isProject = folderContexts.length > 0 && projects.some(p => p.id === folderContexts[0].id |...
      1399         const folderContextText = (folderContexts.length > 0 && !isProject) ? `\n\n[Shared folder context...
      1407         if (!isProject) {                                                                                   
      2366                     onClick={() => !isProjectLocked && setShowProjectDropdown(!showProjectDropdown)}        
      2367                     title={isProjectLocked ? "Project Locked" : "Select Project"}                           
      2368                     style={{ display: "flex", alignItems: "center", gap: 6, background: "transparent", bo...
      2369                     onMouseEnter={e => { if (!isProjectLocked) { e.currentTarget.style.backgroundColor = ...
      2370                     onMouseLeave={e => { if (!isProjectLocked) { e.currentTarget.style.backgroundColor = ...
      2374                     {!isProjectLocked && <ChevronDownIcon width={12} height={12} style={{ marginLeft: 4, ...
      2788                             {missionTimeline && !isProjectTasksPaneOpen && (                                
      2790                                     setIsProjectTasksPaneOpen(true);                                        
      3783                                 {missionTimeline && isProjectTasksPaneOpen && (                             
      3796                                             <button type="button" onClick={() => setIsProjectTasksPaneOpe...


